extends layout

block content
  h1= news
  p Welcome to #{title}

  h2 Sign in:

    form(method='post')
        p
            label Login
            input(type='text' name='login')
        p
            label Password
            input(type='password' name='password')
        p
            input(type='submit' value='Sign In')